2) 이벤트 수집(Event Tracking)

페이지뷰/라우트 변경 자동 이벤트

클릭/탭/더블클릭

스크롤(깊이/속도/구간)

폼 이벤트(포커스/블러/체인지/서브밋)

인풋 입력 “패턴” 이벤트(원문 미저장 전제)

커스텀 이벤트(track)

이벤트 스키마/프로퍼티 관리

UTM/레퍼러/캠페인 자동 수집

에러 이벤트(JS error, unhandled rejection)

퍼포먼스 이벤트(Core Web Vitals/네트워크/리소스)

3) 세션·리플레이(Session Replay)

세션 기록/재생(타임라인)

재생 속도/점프/구간 반복

이벤트 마커(클릭/스크롤/폼/에러/퍼포먼스)

이탈 직전 행동 자동 하이라이트

세션 북마크/하이라이트

세션 공유 링크(권한 기반)

검색/필터로 세션 탐색

4) 좌표 스트림·고급 트레이스(Ghost Trace 계열)

고해상도 좌표 스트림 수집

커서 이동 경로 오버레이

커서 jitter 시각화

dwell(머문 시간) 시각화

복잡한 오버레이 토글(레이어 시스템)

커스텀 selector(요소 매핑) 설정

DOM 변경 대응(리매핑/폴백)

5) 히트맵·오버레이(Heatmap & Overlay)

클릭 히트맵

스크롤 히트맵(깊이/밀도)

무브 히트맵(커서 이동)

영역/요소 단위 집계(컴포넌트/셀렉터 기반)

페이지/라우트별 히트맵

기간 비교(전주/전월/커스텀)

디바이스/해상도별 히트맵

실제 화면 위 오버레이로 “그리듯” 표시(리플레이/히트맵 공통)

6) 분석(Analytics)

실시간 대시보드(Realtime)

핵심 지표 보드(사용자/세션/페이지/전환/이탈)

퍼널 분석(Funnel)

드롭오프 구간 분석

코호트(Cohort)

리텐션(Retention)

세그먼트(Segment)

경로 분석(Path)

전환 기여(간단 어트리뷰션)

변경 전후 비교(A/B Diff: 기간/세그먼트/릴리즈 기준)

7) 세그먼트·필터링

세그먼트 생성/저장/공유

조건 빌더(AND/OR/NOT)

디바이스/OS/브라우저/국가/도시

신규/재방문/리피터

이벤트 프로퍼티 기반 조건

기간/요일/시간대 필터

8) 리포트(Reporting) — “전반” 제공

리포트 빌더(카드/차트/표/퍼널/코호트/히트맵/리플레이 링크 등 구성)

리포트 템플릿(전환/유입/이탈/에러/퍼포먼스/리플레이 요약)

리포트 비교(기간/세그먼트)

리포트 스냅샷 생성(공유 링크/권한)

내보내기(최소 CSV, 필요 시 PDF 포함 가능)

예약 리포트(정기 생성) → Discord로 전송

이상 징후 요약(급등락 Top N, 원인 후보 페이지/이벤트)

9) 알림·모니터링(Alerts) — Discord 단일

Discord Webhook 연동(워크스페이스/프로젝트별)

알림 룰 생성/수정/삭제(임계치/급등락/에러 급증/전환 급락 등)

알림 쿨다운/중복 억제/배치 묶음 전송

메시지 템플릿(지표/기간/비교/스냅샷 링크 포함)

알림 테스트 전송(연동 검증)

실패 재시도/실패 로그

10) 보안·안정성(Safety)- 보안은 cursor 알아서 해- 알아서 편한대로 적당히 좋게게

레이트 리밋(rate limit)

이벤트 스키마 검증/데이터 무결성 체크

봇/스팸 트래픽 필터링

민감정보 마스킹/차단 규칙(입력/URL/프로퍼티)

IP/도메인 기반 제한

데이터 보존 정책(보관 기간)

삭제 요청/데이터 삭제 플로우

12) 데이터 관리(Data Ops) — API 없이 운영 중심

원천 로그/집계 데이터 분리

데이터 품질 모니터링(수집 공백/급감 탐지)

리포트/분석 화면에서 필요한 범위의 데이터 내보내기(Export)
- 보안은 cursor 알아서 해- 알아서 편한대로 적당히 좋게게